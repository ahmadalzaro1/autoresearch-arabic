---
phase: 03-architecture-search
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - results_d3.tsv
  - train.py
autonomous: false
requirements:
  - SRCH-03
must_haves:
  truths:
    - "Branch autoresearch/arabic-d3 exists off main"
    - "results_d3.tsv exists on the branch with 70+ experiment rows"
    - "At least one 'keep' row in results_d3.tsv has val_bpb below 1.075381"
    - "search_results.json has a 'd3' key with best_val_bpb and commit"
  artifacts:
    - path: "results_d3.tsv"
      provides: "D3 condition experiment log, 70+ rows, tab-separated"
      contains: "commit\tval_bpb\tmemory_gb\tstatus\tdescription"
    - path: "search_results.json"
      provides: "Best config per condition — d3 entry written after this plan"
      exports:
        - d3.best_val_bpb
        - d3.commit
  key_links:
    - from: "train.py"
      to: "AUTORESEARCH_CONDITION=d3"
      via: "os.environ.get('AUTORESEARCH_CONDITION', 'd1')"
      pattern: "AUTORESEARCH_CONDITION"
    - from: "results_d3.tsv"
      to: "search_results.json"
      via: "extract_best() reads TSV, writes JSON"
      pattern: "results_d3\\.tsv"
---

<objective>
Run the D3 overnight autoresearch loop. Set up branch autoresearch/arabic-d3 off main, verify tokenizer exists, establish baseline row in results_d3.tsv, then run the autonomous experiment loop until 70+ experiments are logged. After the loop completes, extract the best config and write the d3 entry to search_results.json. This plan covers SRCH-03 (D3 condition, novel contribution, run first).

Purpose: D3 is the experiment's primary claim — atomic PUA encoding should find better architectures than D1/D2. Run first to establish the D3 result before comparison conditions.
Output: Branch autoresearch/arabic-d3 with results_d3.tsv (70+ rows), d3 entry in search_results.json on main.
</objective>

<execution_context>
@/Users/ahmadalzaro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ahmadalzaro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-architecture-search/03-CONTEXT.md
@.planning/phases/03-architecture-search/03-RESEARCH.md
@autoresearch-mlx/program.md

<interfaces>
<!-- Key contracts the executor must know -->

Condition env var:
  AUTORESEARCH_CONDITION=d3 uv run train.py

Tokenizer verification:
  ls ~/.cache/autoresearch-arabic/d3/tokenizer/tokenizer.pkl
  ls ~/.cache/autoresearch-arabic/d3/tokenizer/token_bytes.npy

Baseline targets (from baseline_results.json):
  d3: val_bpb = 1.075381

TSV filename: results_d3.tsv (NOT results.tsv — locked decision)
Branch name: autoresearch/arabic-d3 (NOT autoresearch/mar5 or similar)

Git staging rules (critical — different from program.md monorepo layout):
  CORRECT: git add train.py
  CORRECT: git add results_d3.tsv
  WRONG:   git add autoresearch-mlx/train.py  (train.py is at project root, not inside monorepo subdir)
  WRONG:   git add -A  (may stage .DS_Store, __pycache__, etc.)

TSV row format (tab-separated):
  commit[TAB]val_bpb[TAB]memory_gb[TAB]status[TAB]description
  memory_gb = peak_vram_mb / 1024, rounded to .1f
  status: keep | discard | crash

Extraction script (Pattern 3 from RESEARCH.md):
  import csv, json, subprocess
  from pathlib import Path
  def extract_best(tsv_path, condition):
      best_row = None
      with open(tsv_path, newline="") as f:
          reader = csv.DictReader(f, delimiter="\t")
          for row in reader:
              if row["status"] != "keep": continue
              if best_row is None or float(row["val_bpb"]) < float(best_row["val_bpb"]):
                  best_row = row
      commit = best_row["commit"]
      train_src = subprocess.check_output(["git", "show", f"{commit}:train.py"], text=True)
      config = {"commit": commit, "best_val_bpb": float(best_row["val_bpb"])}
      for var in ["DEPTH", "WINDOW_PATTERN", "HEAD_DIM", "ASPECT_RATIO", "TOTAL_BATCH_SIZE"]:
          for line in train_src.splitlines():
              if line.strip().startswith(f"{var} ="):
                  config[var.lower()] = line.split("=", 1)[1].strip().strip('"')
                  break
      return config
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set up autoresearch/arabic-d3 branch and initialize results_d3.tsv</name>
  <files>results_d3.tsv</files>
  <action>
    Set up the D3 branch and establish the baseline row. Exact steps:

    1. Return to main and ensure working tree is clean:
       git checkout main
       git status  (must be clean — no uncommitted changes)

    2. Create the D3 branch:
       git checkout -b autoresearch/arabic-d3

    3. Verify D3 tokenizer exists (must have both files):
       ls ~/.cache/autoresearch-arabic/d3/tokenizer/tokenizer.pkl
       ls ~/.cache/autoresearch-arabic/d3/tokenizer/token_bytes.npy
       If either is missing, stop and tell the user to run: uv run prepare.py --condition d3

    4. Read baseline target:
       python -c "
       import json, os
       path = os.path.expanduser('~/.cache/autoresearch-arabic/baseline_results.json')
       with open(path) as f: d = json.load(f)
       print(f'd3 baseline: val_bpb={d[\"d3\"][\"val_bpb\"]:.6f}')
       "
       Expected: d3 baseline: val_bpb=1.075381

    5. Initialize results_d3.tsv header:
       printf 'commit\tval_bpb\tmemory_gb\tstatus\tdescription\n' > results_d3.tsv

    6. Run baseline to establish the branch starting point (unmodified train.py):
       AUTORESEARCH_CONDITION=d3 uv run train.py > run.log 2>&1
       grep "^val_bpb:\|^peak_vram_mb:" run.log
       Expected val_bpb: ~1.075381 (within 0.001 of Phase 2 baseline — same code, same hardware)

    7. Extract values and add baseline row to results_d3.tsv:
       - Get commit hash: git rev-parse --short HEAD (this is the baseline commit — train.py unmodified from main)
       - Wait: DO NOT commit train.py yet — it has not been modified. Commit only results_d3.tsv with the baseline row.
       - Add baseline row manually: {commit}\t{val_bpb}\t{memory_gb}\tkeep\tbaseline
       - git add results_d3.tsv && git commit -m "experiment: baseline (d3 condition)"

    NOTE: The baseline commit hash should be the commit that was HEAD before we ran train.py — since train.py was NOT modified, we stage only results_d3.tsv. The commit column in the TSV row should be the HEAD commit at this point (the baseline setup commit). This establishes the reset anchor for the loop.

    val_bpb must be within 0.002 of 1.075381. If it deviates significantly, check that AUTORESEARCH_CONDITION=d3 was set correctly and tokenizer is intact.
  </action>
  <verify>
    <automated>git branch --show-current && wc -l results_d3.tsv && head -3 results_d3.tsv</automated>
  </verify>
  <done>
    Current branch is autoresearch/arabic-d3. results_d3.tsv has 2 lines (header + baseline row). Baseline row has status=keep and description=baseline. val_bpb within 0.002 of 1.075381.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run D3 autonomous experiment loop (70+ experiments)</name>
  <files>train.py, results_d3.tsv</files>
  <action>
    Run the autoresearch experiment loop following program.md exactly. This task runs AUTONOMOUSLY and NEVER STOPS to ask the human. Target: 70+ total experiment rows in results_d3.tsv (header row does not count).

    BEFORE STARTING: Read these files for full context:
    - autoresearch-mlx/program.md (the law — follow exactly)
    - prepare.py (read-only, understand fixed constants)
    - train.py (the file to modify — understand current architecture)

    LOOP until results_d3.tsv has 70+ data rows:

    Each iteration:
    1. Inspect current state: git log --oneline -5, review results_d3.tsv for patterns
    2. Form hypothesis: what architectural change is likely to improve val_bpb?
    3. Modify train.py (ONLY train.py — never touch prepare.py)
    4. Stage and commit: git add train.py && git commit -m "experiment: <description>"
       NOTE: path is train.py (project root), NOT autoresearch-mlx/train.py
    5. Run: AUTORESEARCH_CONDITION=d3 uv run train.py > run.log 2>&1
    6. Extract results: grep "^val_bpb:\|^peak_vram_mb:" run.log
       - Empty output = crash. Run: tail -n 50 run.log. Fix if trivial, else log crash and discard.
    7. Log to results_d3.tsv: {short_commit}\t{val_bpb}\t{memory_gb}\t{status}\t{description}
       - memory_gb = peak_vram_mb / 1024, rounded to .1f
    8a. If improved (lower val_bpb): git add results_d3.tsv && git commit --amend --no-edit
    8b. If equal/worse: record discard hash in TSV, git reset --hard <previous_kept_commit>

    TIMEOUT: If a run exceeds 15 minutes, kill it. Log as crash. Discard.
    NEVER STOP: Do not ask "should I continue?". Do not pause at 10, 20, 50 experiments. Run until 70+ rows exist in results_d3.tsv. The human is likely asleep.
    NEVER USE: git add -A (may stage untracked junk)
    NEVER MODIFY: prepare.py

    Architecture ideas to explore (Claude's discretion — not exhaustive):
    - Depth variations (D3 trains 3.5x more tokens/5min — may benefit from deeper models)
    - Window pattern variations (SSSL, SSLL, SLSL, SLLL, SSSS, LLLL, etc.)
    - Head dimension and aspect ratio changes
    - Optimizer hyperparameters (LR, weight decay, beta values)
    - Batch size variations
    - Combining near-miss improvements
    - Model width vs depth tradeoffs
    - Removing components (simplification wins count)

    STOPPING CONDITION: Count rows in results_d3.tsv (excluding header). When count >= 70, loop is complete.
    FINAL CHECK: grep "^val_bpb:" run.log from the last kept experiment should show a value.
  </action>
  <verify>
    <automated>wc -l results_d3.tsv && awk -F'\t' 'NR>1 && $4=="keep" {print $2}' results_d3.tsv | sort -n | head -1</automated>
  </verify>
  <done>
    results_d3.tsv has 71+ lines (header + 70 data rows). At least one 'keep' row exists. Best keep val_bpb is printed (target: below 1.075381, but search outcome is empirical).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2b: Human verify D3 results</name>
  <action>Human reviews D3 results per verification steps below and approves before Task 3 proceeds.</action>
  <what-built>D3 overnight autoresearch run — 70+ experiments on autoresearch/arabic-d3 branch. results_d3.tsv logged at project root. Best architecture config identified.</what-built>
  <how-to-verify>
    Run these checks:
    1. git branch --show-current (should be autoresearch/arabic-d3)
    2. wc -l results_d3.tsv (should be 71+: header + 70 data rows)
    3. awk -F'\t' 'NR>1 && $4=="keep" {print $2}' results_d3.tsv | sort -n | head -1 (best val_bpb from kept experiments)
    4. uv run pytest tests/test_search.py::test_search_d3 -v (should PASS, not skip)
    5. Eyeball results_d3.tsv: does the search appear to have explored diverse ideas?
    6. Confirm best val_bpb is below 1.075381 (D3 baseline). If not, note it — search does not guarantee improvement, but it is the expected outcome.
  </how-to-verify>
  <resume-signal>Type "d3-approved" if results look good, or describe issues found</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Extract D3 best config and write d3 entry to search_results.json</name>
  <files>search_results.json</files>
  <action>
    After human approves D3 results, extract the best config and write it to search_results.json on main.

    IMPORTANT: Run this while on autoresearch/arabic-d3 branch (git show will need the commits to be reachable). After writing search_results.json, switch to main and commit it there.

    Steps:
    1. Confirm branch: git branch --show-current (must be autoresearch/arabic-d3)

    2. Run extraction script (from RESEARCH.md Pattern 3):
       Write a small Python script extract_d3.py (temporary, will be deleted):

       import csv, json, subprocess
       from pathlib import Path

       def extract_best(tsv_path):
           best_row = None
           with open(tsv_path, newline="") as f:
               reader = csv.DictReader(f, delimiter="\t")
               for row in reader:
                   if row["status"] != "keep":
                       continue
                   if best_row is None or float(row["val_bpb"]) < float(best_row["val_bpb"]):
                       best_row = row
           if best_row is None:
               raise ValueError("No 'keep' rows found in " + str(tsv_path))
           commit = best_row["commit"]
           train_src = subprocess.check_output(["git", "show", f"{commit}:train.py"], text=True)
           config = {"commit": commit, "best_val_bpb": float(best_row["val_bpb"])}
           for var in ["DEPTH", "WINDOW_PATTERN", "HEAD_DIM", "ASPECT_RATIO", "TOTAL_BATCH_SIZE"]:
               for line in train_src.splitlines():
                   if line.strip().startswith(f"{var} ="):
                       config[var.lower()] = line.split("=", 1)[1].strip().strip('"')
                       break
           return config

       d3_result = extract_best("results_d3.tsv")
       print(json.dumps(d3_result, indent=2))

       # Write/merge into search_results.json
       results_path = Path("search_results.json")
       existing = {}
       if results_path.exists():
           existing = json.loads(results_path.read_text())
       existing["d3"] = d3_result
       results_path.write_text(json.dumps(existing, indent=2))
       print(f"Written d3 entry to search_results.json")

       Run: uv run python extract_d3.py
       Delete: rm extract_d3.py

    3. Switch to main and commit search_results.json there:
       git checkout main
       git add search_results.json
       git commit -m "feat(03): add D3 search results to search_results.json"

    PITFALL to avoid: search_results.json is committed on MAIN (not on the autoresearch branch). The autoresearch branch holds train.py history and results_d3.tsv. Main holds the summary artifact for Phase 4.

    Verify the JSON is well-formed: python -c "import json; d=json.load(open('search_results.json')); print(d['d3']['best_val_bpb'], d['d3']['commit'])"
  </action>
  <verify>
    <automated>uv run pytest tests/test_search.py::test_search_results_json -v --tb=short 2>&1 | tail -10</automated>
  </verify>
  <done>
    search_results.json exists on main with a valid "d3" key containing best_val_bpb (float) and commit (str, 7 chars). test_search_results_json passes (even though d1/d2 keys are not yet present, the test asserts all three keys — so this task does NOT need to pass test_search_results_json fully, just confirm d3 data is present and well-formed. The test will skip or show partial failure until all three conditions complete — that is acceptable at this stage).

    Actual done criteria: python -c "import json; d=json.load(open('search_results.json')); print(d['d3'])" prints a dict with best_val_bpb and commit. search_results.json is committed on main.
  </done>
</task>

</tasks>

<verification>
After Task 3: git log --oneline -3 (main) shows search_results.json commit.
python -c "import json; d=json.load(open('search_results.json')); print(d['d3']['best_val_bpb'])" prints a float below 1.075381 (or at worst, equal — empirical).
wc -l results_d3.tsv on autoresearch/arabic-d3 branch shows 71+ lines.
</verification>

<success_criteria>
- Branch autoresearch/arabic-d3 exists off main with 70+ experiment commits
- results_d3.tsv has 70+ data rows with valid tab-separated schema
- At least one 'keep' row has val_bpb below D3 baseline (1.075381) — search outcome is empirical
- search_results.json on main has a "d3" entry with best_val_bpb and commit
- uv run pytest tests/test_search.py::test_search_d3 -v passes (green, not skipped)
</success_criteria>

<output>
After completion, create .planning/phases/03-architecture-search/03-02-SUMMARY.md
</output>
