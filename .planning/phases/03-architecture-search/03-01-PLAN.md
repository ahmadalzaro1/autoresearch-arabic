---
phase: 03-architecture-search
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/test_search.py
autonomous: true
requirements:
  - SRCH-01
  - SRCH-02
  - SRCH-03
must_haves:
  truths:
    - "tests/test_search.py exists and all four stubs are skippable when TSV/JSON are absent"
    - "uv run pytest tests/test_search.py -q exits 0 (all skipped) before overnight runs"
    - "test_search_d3, test_search_d1, test_search_d2 each verify 70+ rows, valid schema, and a 'keep' row below baseline"
    - "test_search_results_json verifies search_results.json has d1/d2/d3 keys with best_val_bpb and commit"
  artifacts:
    - path: "tests/test_search.py"
      provides: "Post-run smoke tests for all three condition TSV files and search_results.json"
      exports:
        - test_search_d1
        - test_search_d2
        - test_search_d3
        - test_search_results_json
  key_links:
    - from: "tests/test_search.py"
      to: "results_d3.tsv, results_d1.tsv, results_d2.tsv"
      via: "csv.DictReader, delimiter='\\t'"
      pattern: "results_d[0-9]\\.tsv"
    - from: "tests/test_search.py"
      to: "search_results.json"
      via: "json.load"
      pattern: "search_results\\.json"
---

<objective>
Create the Wave 0 test scaffold for Phase 3. Write tests/test_search.py with four pytest tests that verify the overnight run artifacts: one test per condition TSV and one for search_results.json. All tests skip gracefully if the artifacts do not yet exist (pre-run state), making them safe to run at any point.

Purpose: Establish the automated verification gate before any overnight runs begin. Each condition's run is only declared complete once its test turns green.
Output: tests/test_search.py with four smoke tests.
</objective>

<execution_context>
@/Users/ahmadalzaro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ahmadalzaro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-architecture-search/03-RESEARCH.md
@.planning/phases/03-architecture-search/03-VALIDATION.md

<interfaces>
<!-- Existing test infrastructure the executor must match -->
<!-- From tests/conftest.py and existing test files -->

tests/ layout:
  tests/__init__.py      (empty)
  tests/conftest.py      (Phase 1/2 fixtures — no Phase 3 fixtures needed)
  tests/test_baseline.py (Phase 2)
  tests/test_pipeline.py (Phase 1)
  tests/test_tokenizer.py (Phase 2)

TSV schema (from program.md / RESEARCH.md):
  columns: commit, val_bpb, memory_gb, status, description
  delimiter: TAB (not comma)
  status values: "keep", "discard", "crash"
  val_bpb for baseline entries: 1.075381 (d3), 1.190999 (d1), 1.596882 (d2)

Baselines to beat:
  d3: 1.075381
  d1: 1.190999
  d2: 1.596882

search_results.json expected shape:
{
  "d3": {"best_val_bpb": float, "commit": str, ...},
  "d1": {"best_val_bpb": float, "commit": str, ...},
  "d2": {"best_val_bpb": float, "commit": str, ...}
}

Project root for TSV files: /Users/ahmadalzaro/Desktop/Karpathy/autoresearch-arabic/
Run command: uv run pytest tests/test_search.py -q
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write tests/test_search.py with four smoke tests</name>
  <files>tests/test_search.py</files>
  <behavior>
    - test_search_d3: if results_d3.tsv absent → pytest.skip("results_d3.tsv not yet created"); else assert row count >= 70; assert all rows have columns [commit, val_bpb, memory_gb, status, description]; assert at least one row has status=="keep" and float(val_bpb) < 1.075381 (D3 baseline)
    - test_search_d1: same pattern, file=results_d1.tsv, baseline=1.190999
    - test_search_d2: same pattern, file=results_d2.tsv, baseline=1.596882
    - test_search_results_json: if search_results.json absent → pytest.skip("search_results.json not yet created"); else load json; assert "d1", "d2", "d3" all present as keys; for each key assert "best_val_bpb" (float) and "commit" (str, len 7) are present
    - Row count = total rows (all statuses count) — matches program.md intent (70 experiment attempts)
    - TSV files live at project root (Path(__file__).parent.parent / "results_d3.tsv")
    - search_results.json lives at project root
    - Header row does NOT count toward the 70
  </behavior>
  <action>
    Create tests/test_search.py. Import pathlib.Path, csv, json, pytest.

    File layout:
    1. MODULE-LEVEL CONSTANTS: BASELINES = {"d3": 1.075381, "d1": 1.190999, "d2": 1.596882}. PROJECT_ROOT = Path(__file__).parent.parent.

    2. HELPER: def _read_tsv(path: Path) -> list[dict] — opens with csv.DictReader(f, delimiter="\t"), returns list of dicts. If path does not exist, returns None.

    3. def _check_condition(condition: str) — reads results_{condition}.tsv; if None, pytest.skip(f"results_{condition}.tsv not yet created — run D{condition[-1].upper()} overnight search first"). Asserts:
       - len(rows) >= 70 (total rows including header already excluded by DictReader)
       - All rows have keys: {"commit", "val_bpb", "memory_gb", "status", "description"}
       - At least one row with status=="keep" and float(row["val_bpb"]) < BASELINES[condition]

    4. Three condition tests calling _check_condition:
       - def test_search_d3(): _check_condition("d3")
       - def test_search_d1(): _check_condition("d1")
       - def test_search_d2(): _check_condition("d2")

    5. def test_search_results_json():
       - path = PROJECT_ROOT / "search_results.json"
       - if not path.exists(): pytest.skip("search_results.json not yet created")
       - data = json.loads(path.read_text())
       - assert set(data.keys()) >= {"d1", "d2", "d3"}
       - for cond in ["d1", "d2", "d3"]:
           assert "best_val_bpb" in data[cond], f"missing best_val_bpb for {cond}"
           assert isinstance(data[cond]["best_val_bpb"], float), f"best_val_bpb not float for {cond}"
           assert "commit" in data[cond], f"missing commit for {cond}"
           assert isinstance(data[cond]["commit"], str) and len(data[cond]["commit"]) >= 7

    RED phase: Run uv run pytest tests/test_search.py -q immediately after writing. All four tests must SKIP (not fail) since TSV/JSON do not exist yet. If any test fails (not skips), fix the skip logic before continuing. Skip = pass in this Wave 0 context.

    No external imports. No fixtures from conftest. Standalone module.
  </action>
  <verify>
    <automated>uv run pytest tests/test_search.py -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>All four tests show "SKIPPED" (not FAILED, not ERROR). Exit code 0. uv run pytest tests/test_search.py -q shows "4 skipped" with no failures.</done>
</task>

</tasks>

<verification>
uv run pytest tests/test_search.py -v shows 4 tests, all SKIPPED, exit code 0.
uv run pytest tests/ -q shows all existing tests still green (no regressions in Phase 1/2 tests).
</verification>

<success_criteria>
- tests/test_search.py exists with four named tests: test_search_d3, test_search_d1, test_search_d2, test_search_results_json
- All four skip gracefully when artifacts are absent (pre-run state)
- TSV validation logic checks: 70+ total rows, required column schema, at least one keep row below baseline
- search_results.json validation checks: d1/d2/d3 keys, best_val_bpb (float), commit (str, 7+ chars)
- Full test suite (tests/) still passes with no regressions
</success_criteria>

<output>
After completion, create .planning/phases/03-architecture-search/03-01-SUMMARY.md
</output>
