---
phase: 01-data-pipeline
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - build_dataset.py
  - validate_dataset.py
autonomous: false
requirements:
  - DATA-02
  - DATA-03
  - DATA-04

must_haves:
  truths:
    - "After build_dataset.py runs, all four validation checks run for each condition and print PASS/FAIL"
    - "validation_report.json is written to ~/.cache/autoresearch-arabic/ after each condition builds"
    - "uv run python validate_dataset.py re-validates all conditions without rebuilding data"
    - "validate_dataset.py exits 1 if any mandatory check fails"
    - "D3 PUA integrity gate calls sys.exit(1) if atomic_mapping.json has fewer than 252 entries"
    - "Character distribution prints top-20 most frequent chars per condition as a visual-only assertion"
  artifacts:
    - path: "build_dataset.py"
      provides: "validate_condition() and write_validation_report() functions; main() calls them inline"
      contains: "validate_condition"
    - path: "validate_dataset.py"
      provides: "Standalone re-validator; imports validate_condition from build_dataset"
    - path: "~/.cache/autoresearch-arabic/validation_report.json"
      provides: "Per-condition pass/fail record consumed by Phase 2"
  key_links:
    - from: "build_dataset.py:main()"
      to: "validate_condition()"
      via: "called after each process_condition() in the conditions loop"
      pattern: "validate_condition"
    - from: "validate_condition()"
      to: "validation_report.json"
      via: "write_validation_report() merges per-condition result into JSON"
      pattern: "write_validation_report"
    - from: "validate_dataset.py:main()"
      to: "validate_condition()"
      via: "from build_dataset import validate_condition"
      pattern: "from build_dataset import"
---

<objective>
Add inline validation to build_dataset.py (auto-runs after each condition build) and create
validate_dataset.py as a standalone re-validator. Both write validation_report.json.

Purpose: DATA-02/03/04 require verified shards before Phase 2. The four mandatory checks
(shard loadability, row count, zero empty texts, character distribution) and the D3 PUA
integrity gate ensure data integrity. A human checkpoint confirms the visual char distribution.
Output: Updated build_dataset.py with validate_condition() and write_validation_report();
new validate_dataset.py standalone script.
</objective>

<execution_context>
@/Users/ahmadalzaro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ahmadalzaro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-data-pipeline/01-CONTEXT.md
@.planning/phases/01-data-pipeline/01-RESEARCH.md
@build_dataset.py
@.planning/phases/01-data-pipeline/01-02-SUMMARY.md

<!-- Key constants and interfaces already in build_dataset.py after Plan 02: -->
<!--   BASE_CACHE, HARAKAT_RE, json (top-level import), process_condition() -->
<!--   metadata.txt format: condition=, train_docs=, val_docs=, val_shard=, val_filename= -->
<!--   conditions loop in main(): for condition in conditions: process_condition(...) -->
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add validate_condition() and write_validation_report() to build_dataset.py</name>
  <files>build_dataset.py</files>
  <behavior>
    - validate_condition(condition) runs 4 checks: shards_loadable, row_count_matches, no_empty_texts, char_distribution
    - Checks 1-3 print PASS/FAIL and call sys.exit(1) on failure
    - Check 4 (char_distribution) prints top-20 chars to stdout but never hard-fails
    - D3 extra gate: if condition == "d3", load atomic_mapping.json, sys.exit(1) if len(mapping) less than 252
    - write_validation_report(condition, results) reads existing report if present, updates condition key, writes back with ensure_ascii=False
    - main() calls validate_condition(condition) then write_validation_report(condition, val_results) after each process_condition() in the loop
    - No --skip-validation flag (validation always runs)
  </behavior>
  <action>
Add a Validation section to build_dataset.py between the dataset processing section and Main.
Add import sys at the top of the file with the other stdlib imports.

Add _compute_char_distribution(condition, data_dir):
- Use collections.Counter to count all characters across all shards
- Print: "  [{condition}] Top-20 chars: {top-10 sample} ..."
- Return dict mapping repr(char) -> count for the top-20

Add validate_condition(condition) -> dict:
- data_dir = BASE_CACHE / condition / "data"
- meta_path = BASE_CACHE / condition / "metadata.txt"
- Hard-fail if data_dir or meta_path do not exist
- shards = sorted(data_dir.glob("shard_*.parquet"))
- Hard-fail if no shards found
- Check 1 shards_loadable: wrap each pq.read_table(shard) in try/except; set load_ok=False on any error; sys.exit(1) if not load_ok
- Check 2 row_count_matches: parse metadata.txt with split("=", 1); separate train vs val shards using val_filename from meta; sum actual rows; sys.exit(1) if mismatch
- Check 3 no_empty_texts: loop all shards, check any(t is None or t == "" for t in col); sys.exit(1) if found
- Check 4 char_distribution: call _compute_char_distribution(condition, data_dir), store result
- D3 gate (condition == "d3"): load BASE_CACHE / "atomic_mapping.json", sys.exit(1) if len(mapping) < 252
- Print "  [{condition}] All mandatory checks PASSED"
- Return results dict with keys: shards_loadable, row_count_matches, no_empty_texts, char_distribution

Add write_validation_report(condition, results):
- report_path = BASE_CACHE / "validation_report.json"
- Load existing JSON if file exists (preserves other conditions already written)
- Update report[condition] = results
- Write with json.dump(..., ensure_ascii=False, indent=2)
- Print "  Validation report updated: {report_path}"

Update the conditions loop in main():
  Before: process_condition(condition, ...)
  After:  process_condition(condition, ...)
          val_results = validate_condition(condition)
          write_validation_report(condition, val_results)
  </action>
  <verify>
    <automated>uv run python -c "import build_dataset, inspect; [print(f'PASS: {fn}') or None for fn in ['validate_condition','write_validation_report','_compute_char_distribution'] if hasattr(build_dataset, fn)]; src=inspect.getsource(build_dataset.main); assert 'validate_condition' in src; assert 'write_validation_report' in src; print('PASS: main() calls both')" && uv run pytest tests/test_pipeline.py -q</automated>
  </verify>
  <done>
    validate_condition(), write_validation_report(), _compute_char_distribution() exist in build_dataset.py.
    main() calls validate_condition(condition) and write_validation_report(condition, val_results) inside the conditions loop.
    uv run pytest tests/test_pipeline.py -q exits 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create validate_dataset.py standalone validator</name>
  <files>validate_dataset.py</files>
  <behavior>
    - uv run python validate_dataset.py validates all three conditions (d1, d2, d3)
    - uv run python validate_dataset.py --condition d1 validates only D1
    - Exits 0 if all requested conditions pass all mandatory checks
    - Exits 1 if any mandatory check fails
    - Prints "Validation complete: N/M conditions passed" as the final line
    - Imports validate_condition and write_validation_report from build_dataset (no logic duplication)
    - Catches SystemExit from validate_condition to continue checking remaining conditions before final exit
  </behavior>
  <action>
Create validate_dataset.py at the project root with:
- Module docstring explaining purpose and usage
- Imports: argparse, sys, and "from build_dataset import validate_condition, write_validation_report"
- main() function with argparse for --condition (choices: d1, d2, d3, all; default: all)
- Loop over conditions, call validate_condition(cond) inside try/except SystemExit to catch hard-fails
- Track passed and failed counts
- After loop, print "Validation complete: {passed}/{total} conditions passed"
- Call sys.exit(1) if failed > 0
- if __name__ == "__main__": main()

The SystemExit catch is necessary because validate_condition() calls sys.exit(1) on hard fails.
By catching it, the script can continue to the next condition and show a complete failure report.
  </action>
  <verify>
    <automated>cd /Users/ahmadalzaro/Desktop/Karpathy/autoresearch-arabic && uv run python -c "import ast, pathlib; src=pathlib.Path('validate_dataset.py').read_text(); assert 'validate_condition' in src; assert 'from build_dataset import' in src; assert 'argparse' in src; print('PASS: validate_dataset.py structure correct')" && uv run python validate_dataset.py --help</automated>
  </verify>
  <done>
    validate_dataset.py exists and imports without error.
    uv run python validate_dataset.py --help shows usage with --condition option.
    from build_dataset import validate_condition is used (no logic duplication).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify validation suite output and character distributions</name>
  <what-built>
    Full validation suite: build_dataset.py now runs 4 mandatory checks inline after each
    condition build. validate_dataset.py re-validates without rebuilding. Both write
    validation_report.json.
  </what-built>
  <how-to-verify>
    After running build_dataset.py (or if cache is already warm from a prior run):

    1. Run: uv run python validate_dataset.py
       Expected: prints PASS/FAIL per check for each condition; exits 0 if all good
       Check: "All mandatory checks PASSED" appears for d1, d2, and d3

    2. Inspect character distribution output:
       - D1 output should show Arabic combining marks (harakah chars like fathah U+064E) in top-20
       - D2 output should show NO harakah chars (they were stripped)
       - D3 output should show PUA chars (codepoints in E000-EFFF range) prominently

    3. Check validation_report.json exists:
       cat ~/.cache/autoresearch-arabic/validation_report.json
       Expected: JSON with d1, d2, d3 keys, each containing shards_loadable=true, row_count_matches=true, no_empty_texts=true

    If data has not been built yet, run:
       uv run python build_dataset.py --max-examples 1000
    This builds a small test dataset (~1000 rows) to exercise the full pipeline including validation.
  </how-to-verify>
  <action>Human verification: run validate_dataset.py and inspect stdout output as described in how-to-verify above.</action>
  <verify><automated>HUMAN_ONLY — visual assertion, no automated command</automated></verify>
  <done>Character distributions confirmed: D1 has harakat, D2 has none, D3 has PUA chars; validation_report.json all conditions pass.</done>
  <resume-signal>Type "approved" if char distributions look correct, or describe any issues seen</resume-signal>
</task>

</tasks>

<verification>
uv run python -c "import build_dataset" exits 0.
uv run pytest tests/test_pipeline.py -q exits 0 (all 3 fixture tests pass, 3 skip cleanly).
uv run python validate_dataset.py --help exits 0.
validate_dataset.py imports from build_dataset (no duplicated logic).
build_dataset.py conditions loop calls validate_condition() and write_validation_report() after each condition.
</verification>

<success_criteria>
- validate_condition(), write_validation_report(), _compute_char_distribution() exist in build_dataset.py
- main() in build_dataset.py calls validation functions after each process_condition()
- validate_dataset.py exists, imports from build_dataset, supports --condition flag
- uv run pytest tests/test_pipeline.py -q exits 0
- Human has confirmed character distribution visual checks look correct
</success_criteria>

<output>
After completion, create .planning/phases/01-data-pipeline/01-03-SUMMARY.md
</output>
