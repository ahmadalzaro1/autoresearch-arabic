---
phase: 02-tokenizer-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/conftest.py
  - tests/test_tokenizer.py
  - tests/test_baseline.py
autonomous: true
requirements: [TOK-01, TOK-02, TOK-03, TOK-04, BASE-01, BASE-02, BASE-03]

must_haves:
  truths:
    - "pytest discovers and collects tests from test_tokenizer.py and test_baseline.py without ImportError"
    - "All 7 test stubs are present and either skip (integration) or fail RED on missing code"
    - "tiny_corpus_parquet fixture writes two parquet shards to tmp_path with at least 10 Arabic text rows each and a metadata.txt"
    - "test_vocab_size_flag fails RED because prepare.py does not yet accept --vocab-size"
    - "test_fertility_report_written fails RED because write_fertility_report() does not yet exist in prepare.py"
    - "test_baseline_json_written fails RED because baseline_results.json writer does not yet exist in train.py"
  artifacts:
    - path: "tests/test_tokenizer.py"
      provides: "Stub tests for TOK-01 through TOK-04"
      exports: [test_vocab_size_flag, test_fertility_report_written, test_tokenizer_files_exist, test_fertility_table_conditions]
    - path: "tests/test_baseline.py"
      provides: "Stub tests for BASE-01 through BASE-03"
      exports: [test_baseline_json_written, test_d2_lower_than_d1, test_baseline_d3]
    - path: "tests/conftest.py"
      provides: "tiny_corpus_parquet fixture for tokenizer unit tests"
      contains: "tiny_corpus_parquet"
  key_links:
    - from: "tests/test_tokenizer.py"
      to: "prepare.py"
      via: "subprocess.run(['uv', 'run', 'prepare.py', '--vocab-size', ...])"
      pattern: "vocab.size|write_fertility_report"
    - from: "tests/test_baseline.py"
      to: "~/.cache/autoresearch-arabic/baseline_results.json"
      via: "json.load(open(BASE_CACHE / 'baseline_results.json'))"
      pattern: "baseline_results.json"
---

<objective>
Create Nyquist-compliant test stubs for all Phase 2 requirements. These stubs fail RED on missing code (prepare.py --vocab-size, write_fertility_report, baseline_results.json) and provide the test framework and fixtures that Plans 02 and 03 will make go GREEN.

Purpose: Establish automated verification before writing implementation. Every requirement must have an automated test command per the Nyquist rule.
Output: tests/test_tokenizer.py (4 tests), tests/test_baseline.py (3 tests), updated tests/conftest.py (tiny_corpus_parquet fixture).
</objective>

<execution_context>
@/Users/ahmadalzaro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ahmadalzaro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-tokenizer-baseline/02-RESEARCH.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from existing codebase. -->

From tests/conftest.py (existing — must not break existing fixtures):
- ARABIC_SAMPLE_TEXTS: list of 100 diacritized Arabic strings (10 base * 10)
- HARAKAT_RE, HARAKAT_CODEPOINTS, PUA_BASE, ARABIC_LETTERS — shared constants
- Fixtures: tiny_arabic_texts, cache_base, d1_shard_path, d2_shard_path, d3_shard_path
- _write_shard(path, texts) -> Path: writes parquet file with {"text": texts} schema
- _build_atomic_mapping() -> dict: letter+harakah -> PUA codepoint

From prepare.py (current state — these are the gaps Plan 02 will fill):
- VOCAB_SIZE = 8192  (module-level constant, NOT yet a CLI arg)
- BASE_CACHE = ~/.cache/autoresearch-arabic
- train_tokenizer(): reads TOKENIZER_DIR, writes tokenizer.pkl + token_bytes.npy
  - Has early return if tokenizer.pkl + token_bytes.npy already exist
- TOKENIZER_DIR: set by init_condition(condition) to {cache_dir}/tokenizer
- write_fertility_report(): DOES NOT EXIST YET (Plan 02 adds it)

From train.py (current state — baseline_results.json writer missing):
- Writes val_bpb to stdout after evaluate_bpb()
- baseline_results.json writer: DOES NOT EXIST YET (Plan 02 adds it)
- DEPTH = 4, WINDOW_PATTERN = "SSSL" (module-level constants)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add tiny_corpus_parquet fixture to conftest.py</name>
  <files>tests/conftest.py</files>
  <behavior>
    - tiny_corpus_parquet(tmp_path, condition) writes two parquet shards at tmp_path/{condition}/data/shard_00000.parquet and shard_00001.parquet, each with 50 rows of Arabic text appropriate for the condition (d1: diacritized, d2: harakat-stripped, d3: PUA-encoded via _build_atomic_mapping)
    - It also writes tmp_path/{condition}/metadata.txt with val_shard=1 so prepare.py's get_val_shard() finds the val shard correctly
    - Returns the condition data directory path (tmp_path/{condition}/data)
    - Fixture is parametrizable: takes condition as a parameter (not pytest-parametrize — just a regular function fixture that callers invoke with a condition string)
    - Must not break any existing tests in test_pipeline.py — only ADD to conftest.py
  </behavior>
  <action>
    Append to tests/conftest.py (do NOT modify existing fixtures or constants):

    Add a new fixture `tiny_corpus_parquet` that accepts `tmp_path` and a `condition` string argument. Use a factory pattern — the fixture yields a callable `make_corpus(condition)` so individual test functions can call it with the condition they need.

    Implementation:
    - `make_corpus(condition)` creates tmp_path/{condition}/data/ directory
    - Writes shard_00000.parquet (50 rows, train shard) with {"text": [list of Arabic texts]}
    - Writes shard_00001.parquet (50 rows, val shard) with same format
    - For d1: use ARABIC_SAMPLE_TEXTS[:50] for each shard
    - For d2: use [HARAKAT_RE.sub('', t) for t in ARABIC_SAMPLE_TEXTS[:50]]
    - For d3: apply _build_atomic_mapping() encoding to ARABIC_SAMPLE_TEXTS[:50]
    - Writes tmp_path/{condition}/metadata.txt with content: "condition={condition}\ntrain_docs=50\nval_docs=50\ntrain_shards=1\nval_shard=1\nval_filename=shard_00001.parquet\n"
    - Returns tmp_path (the cache base root, so prepare.init_condition can use it via monkeypatching BASE_CACHE)

    Fixture signature:
    ```python
    @pytest.fixture
    def tiny_corpus_parquet(tmp_path):
        def make_corpus(condition: str) -> Path:
            ...
        return make_corpus
    ```

    Use pathlib.Path throughout. Use _write_shard() already defined in conftest.py. Python type hints required.
  </action>
  <verify>
    <automated>cd /Users/ahmadalzaro/Desktop/Karpathy/autoresearch-arabic && uv run pytest tests/test_pipeline.py -q 2>&1 | tail -5</automated>
  </verify>
  <done>Existing test_pipeline.py passes (all previously-passing tests still pass). tiny_corpus_parquet fixture is importable and callable in test context.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create tests/test_tokenizer.py with RED stubs for TOK-01 to TOK-04</name>
  <files>tests/test_tokenizer.py</files>
  <behavior>
    - test_vocab_size_flag: RED — calls `uv run prepare.py --vocab-size 4096 --condition d1 --help` and asserts "--vocab-size" in stdout. Fails RED because prepare.py does not yet accept --vocab-size.
    - test_fertility_report_written: RED — asserts write_fertility_report is importable from prepare. Fails RED because function does not exist.
    - test_tokenizer_files_exist: SKIP (integration) — checks ~/.cache/autoresearch-arabic/d1/tokenizer/tokenizer.pkl exists. Marked @pytest.mark.skip(reason="integration: run after uv run prepare.py --condition d1").
    - test_fertility_table_conditions: SKIP (integration) — reads ~/.cache/autoresearch-arabic/fertility_report.json, asserts d1/d2/d3 keys present with float values. Marked @pytest.mark.skip(reason="integration: run after tokenizer sweep completes").
    - test_d3_tokenizer_roundtrip: SKIP (integration) — reads d3 tokenizer, encodes a known D3-encoded string (PUA chars), checks roundtrip. Marked @pytest.mark.skip(reason="integration: run after uv run prepare.py --condition d3").
  </behavior>
  <action>
    Create /Users/ahmadalzaro/Desktop/Karpathy/autoresearch-arabic/tests/test_tokenizer.py:

    ```
    """
    Tokenizer tests for Phase 2 (TOK-01 through TOK-04).
    Unit tests fail RED until Plan 02 extends prepare.py.
    Integration tests are skipped until tokenizer training runs complete.
    """
    import json
    import subprocess
    import sys
    from pathlib import Path
    import pytest

    BASE_CACHE = Path.home() / ".cache" / "autoresearch-arabic"
    PROJECT_ROOT = Path(__file__).parent.parent
    ```

    test_vocab_size_flag:
    - Run `subprocess.run(["uv", "run", "prepare.py", "--help"], capture_output=True, text=True, cwd=PROJECT_ROOT)`
    - Assert "--vocab-size" in result.stdout — this FAILS RED until Plan 02 adds the flag.

    test_fertility_report_written:
    - Import prepare, assert hasattr(prepare, "write_fertility_report") — FAILS RED until Plan 02 adds the function.
    - Also assert callable(prepare.write_fertility_report)

    test_tokenizer_files_exist (skipped integration):
    - For each condition in ["d1", "d2", "d3"]: assert (BASE_CACHE / condition / "tokenizer" / "tokenizer.pkl").exists()
    - Also assert (BASE_CACHE / condition / "tokenizer" / "token_bytes.npy").exists()

    test_fertility_table_conditions (skipped integration):
    - Load BASE_CACHE / "fertility_report.json"
    - Assert "d1", "d2", "d3" all keys
    - For each condition, assert "8192" is a key with float value between 0.5 and 5.0
    - Assert fertility["d3"]["8192"] < fertility["d1"]["8192"] (D3 should be lower — PUA encoding is more compact)

    test_d3_tokenizer_roundtrip (skipped integration):
    - Load d3 tokenizer.pkl via pickle.load
    - Build a short D3 string: use a hardcoded PUA codepoint sequence (chr(0xE000) + chr(0xE001) + " " + chr(0xE002))
    - enc.decode(enc.encode_ordinary(test_d3)) == test_d3

    All with type hints. Python f-strings. pathlib throughout.
  </action>
  <verify>
    <automated>cd /Users/ahmadalzaro/Desktop/Karpathy/autoresearch-arabic && uv run pytest tests/test_tokenizer.py -q 2>&1 | tail -10</automated>
  </verify>
  <done>test_vocab_size_flag FAILS (RED — "--vocab-size" not in prepare.py --help yet). test_fertility_report_written FAILS (RED — function missing). 3 integration tests SKIP. Pytest exits with failures for the 2 RED tests, 3 skipped.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create tests/test_baseline.py with RED stubs for BASE-01 to BASE-03</name>
  <files>tests/test_baseline.py</files>
  <behavior>
    - test_baseline_json_written: RED — asserts that train.py source code contains "baseline_results.json" string. Fails RED because train.py does not yet write the JSON.
    - test_d2_lower_than_d1: SKIP (integration) — reads baseline_results.json, asserts d2 val_bpb < d1 val_bpb.
    - test_baseline_d3: SKIP (integration) — reads baseline_results.json, asserts d3 key exists with val_bpb in [1.0, 10.0].
    - test_baseline_schema: SKIP (integration) — reads baseline_results.json, for each condition checks required keys: val_bpb, depth, vocab_size, num_params_M, training_seconds, total_tokens_M, window_pattern, timestamp.
  </behavior>
  <action>
    Create /Users/ahmadalzaro/Desktop/Karpathy/autoresearch-arabic/tests/test_baseline.py:

    ```
    """
    Baseline training tests for Phase 2 (BASE-01 through BASE-03).
    Unit test fails RED until Plan 02 extends train.py.
    Integration tests are skipped until baseline training runs complete (3 x 300s).
    """
    import json
    import inspect
    from pathlib import Path
    import pytest

    BASE_CACHE = Path.home() / ".cache" / "autoresearch-arabic"
    PROJECT_ROOT = Path(__file__).parent.parent
    REQUIRED_KEYS = ["val_bpb", "depth", "vocab_size", "num_params_M",
                     "training_seconds", "total_tokens_M", "window_pattern", "timestamp"]
    ```

    test_baseline_json_written:
    - Read train.py source via `(PROJECT_ROOT / "train.py").read_text(encoding="utf-8")`
    - Assert "baseline_results.json" in source — FAILS RED until Plan 02 adds the writer.

    test_d2_lower_than_d1 (skipped integration):
    - Load BASE_CACHE / "baseline_results.json"
    - Assert "d1" in results and "d2" in results
    - d2_bpb = results["d2"]["val_bpb"], d1_bpb = results["d1"]["val_bpb"]
    - Assert d2_bpb < d1_bpb, f"D2 val_bpb ({d2_bpb}) must be lower than D1 val_bpb ({d1_bpb}) — stripping reduces surface complexity"

    test_baseline_d3 (skipped integration):
    - Load baseline_results.json
    - Assert "d3" in results
    - val_bpb = results["d3"]["val_bpb"]
    - Assert 1.0 < val_bpb < 10.0, f"D3 val_bpb={val_bpb} is outside plausible range"

    test_baseline_schema (skipped integration):
    - Load baseline_results.json
    - For each condition in ["d1", "d2", "d3"]:
      - Assert condition in results
      - For each key in REQUIRED_KEYS: assert key in results[condition]
      - Assert results[condition]["depth"] == 4
      - Assert results[condition]["window_pattern"] == "SSSL"

    All with type hints, f-strings, pathlib. Mark all integration tests:
    @pytest.mark.skip(reason="integration: run after AUTORESEARCH_CONDITION=d{1,2,3} uv run train.py completes")
  </action>
  <verify>
    <automated>cd /Users/ahmadalzaro/Desktop/Karpathy/autoresearch-arabic && uv run pytest tests/test_baseline.py -q 2>&1 | tail -10</automated>
  </verify>
  <done>test_baseline_json_written FAILS (RED — "baseline_results.json" not in train.py source yet). 3 integration tests SKIP. Full test suite still runs: uv run pytest tests/ -q shows test_tokenizer.py (2 fail, 3 skip) + test_baseline.py (1 fail, 3 skip) + test_pipeline.py (all pass).</done>
</task>

</tasks>

<verification>
Run full suite after all three tasks:

```bash
cd /Users/ahmadalzaro/Desktop/Karpathy/autoresearch-arabic && uv run pytest tests/ -q
```

Expected: test_pipeline.py all green, test_tokenizer.py 2 fail + 3 skip, test_baseline.py 1 fail + 3 skip. The 3 RED failures confirm Wave 0 is correct — they document exactly what Plan 02 must implement.
</verification>

<success_criteria>
- tests/conftest.py has tiny_corpus_parquet fixture (factory pattern)
- tests/test_tokenizer.py exists with 5 tests: 2 RED (unit), 3 SKIP (integration)
- tests/test_baseline.py exists with 4 tests: 1 RED (unit), 3 SKIP (integration)
- uv run pytest tests/test_pipeline.py -q: all green (no regressions)
- uv run pytest tests/ -q: shows 3 RED failures (not more, not fewer) + 6 skips
- Every requirement ID TOK-01..TOK-04, BASE-01..BASE-03 has a corresponding named test
</success_criteria>

<output>
After completion, create `.planning/phases/02-tokenizer-baseline/02-01-SUMMARY.md`
</output>
